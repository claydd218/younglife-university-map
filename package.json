{
  "name": "younglife-university-map",
  "private": true,
  "description": "Dev tooling only — the site itself has no build step. wrangler runs the admin CMS's Cloudflare Pages Functions locally; production deploys still serve the raw repo unchanged (see admin/SETUP.md).",
  "devDependencies": {
    "wrangler": "^4"
  },
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-09-23"
  }
}
