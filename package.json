{
  "name": "younglife-university-map",
  "private": true,
  "description": "Dev tooling only — the site itself has no build step. wrangler runs the admin CMS Worker locally (see wrangler.toml, admin/SETUP.md); production deploys still serve the rest of the repo as static assets unchanged.",
  "devDependencies": {
    "wrangler": "^4.123.0"
  },
  "scripts": {
    "dev": "wrangler dev"
  }
}
